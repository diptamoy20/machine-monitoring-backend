version: '3.8'

services:
  api:
    build: .
    container_name: machine_monitoring_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/static:/app/app/static
    restart: always
